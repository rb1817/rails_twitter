
<table class="table table-dark">
  <tbody>
     <% @boards.reverse.each do |board|%>
    <tr>
      <td><h3><%=board.contents%></h3></td>
      <td><a href = '/<%=board.id%>/edit'  class = "btn btn-info">수정</a>  <a data-confirm="삭제하겠습니까?" href = '/<%=board.id%>/destroy'  class = "btn btn-danger">삭제</a></td>
    </tr>
    <%end%>
  </tbody>
</table>

